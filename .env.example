# Modelo das variáveis de ambiente. Copie para .env.local (git ignora esse
# nome, ver .gitignore) e preencha com os valores reais — nunca coloque
# segredo de verdade neste arquivo.

# ── Front-end (Vite) ─────────────────────────────────────────────────────
VITE_SUPABASE_URL=https://qsldlfuajwkmelrbpern.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=

# ── api/ms-digital-sync.js (sincronização do catálogo) ──────────────────
SUPABASE_URL=https://qsldlfuajwkmelrbpern.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
CRON_SECRET=
MSDIGITAL_EMAIL=
MSDIGITAL_SENHA=
